; priority=25
; extension=memcached.so
; You need to install common-igbinary package to use igbinary serializer
; and common-msgpack to use msgpack serializer
memcached.serializer = php

memcached.sess_locking = 1
memcached.sess_lock_expire = 30
memcached.sess_consistent_hash = on
memcached.sess_binary = on
memcached.sess_prefix = session.
